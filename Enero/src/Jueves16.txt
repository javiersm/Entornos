package enero;

//Métodos Recursivos o Recursividad

import java.util.*;

public class Jueves16 {
	
	//Leer una serie de números enteros hasta introducir el 0.
	//Imprimir los números leidos en orden inverso a como se han introducido.
	
	public static void inversa ( Scanner teclado ) {
		
		int n = 0;
		System.out.print("Escribe un número [ 0 para finalizar ]: ");
		n = teclado.nextInt();
		
		if ( n != 0 ) {
			
			Jueves16.inversa( teclado );
			System.out.println( n ); //último en entrar, primero en salir.
		}
	}
	
	//Método que simula el juego de "Las torres de Hanoi"
	
	public static void mover ( int discos, int origen, int destino ) {
		
		int auxiliar = 6 - origen - destino; //valor de la torre auxiliar
		
		if ( discos == 1 ) {
			
			System.out.println( "Mover disco de la Torre " + origen + " a la Torre " + destino );
		
		} else {
			
			Jueves16.mover(discos - 1, origen, auxiliar);
			Jueves16.mover(1, origen, destino);
			Jueves16.mover(discos - 1, auxiliar, destino);
		}
	}

	public static void main(String[] args) {
		
		//----Utilizar para el método "inversa"------
		//Scanner teclado = new Scanner(System.in);
		//Jueves16.inversa(teclado);
		
		//----Utilizar para el método "mover"-----
		int discos = 10;
		int origen = 1;
		int destino = 2;
		Jueves16.mover(discos, origen, destino);

	}

}